# Orchester 编码智能体最终规格与当前状态

> 文档阶段：当前收尾阶段
>
> 文档性质：最终现状规格、验收口径与提交状态说明
>
> 证据边界：以主分支、`task/course-closure` 工作树、Git 历史、`README.md`、`AGENT_LOG.md`、课程两份要求及本地验证记录为准；不把未取得的外部结果写成完成。

## 1. 产品定位

Orchester 是一个编码智能体。它的主能力是：理解用户给出的开发任务，组织上下文和计划，选择并执行受治理的文件或命令动作，根据 lint/test 等客观反馈继续修正，并在危险动作需要人工决定时持久化暂停、审批后恢复。调用 Codex、Claude Code、OpenCode 等外部智能体是可选委派能力，不是产品主循环，也不是核心贡献的替代品。

核心工程贡献是由本项目代码拥有的 agent loop 与治理层：单步模型调用、严格动作解码、策略判定、工作区围栏、持久审批、工具执行、反馈回灌、记忆、停止条件和审计事件均由 Orchester 协调。现成 agent runner 不拥有该循环。

相关 Git 提交与 `task/course-closure` 工作树已包含自研运行时、交互式 CLI、真实模型提供方边界、受治理文件/命令动作、验证器门禁、审批恢复、项目记忆、确定性机制演示、本地模拟 WebUI、容器与分发脚本以及 GitHub Actions 配置。主分支尚未合并收尾工作树的全部修订，因此它仍不是可宣称“全部交付完成”的发布版本：规范总门禁当前在服务端 clippy 的 `too_many_arguments` 报错处失败，且公网 URL、托管 CI 通过记录、正式发布/证明和授权远程主机验收证据均不存在。

## 2. 问题、用户与价值

通用编码智能体容易把模型的“完成”当成工程完成，也容易在命令执行、越界路径、审批漂移、失败重试、秘密泄漏和恢复语义上留下不可审计的隐含状态。Orchester 要解决的问题不是再包装一次模型聊天，而是让一个 Coding Agent 的动作可限制、可恢复、可验证、可解释。

目标用户包括：

- 希望 agent 自主处理真实仓库任务、同时保留控制权的开发者。
- 需要检查策略、审批、工具结果和验证证据的 reviewer。
- 需要离线、无真实 API Key 地复现核心机制的课程评审者。
- 仍需使用既有外部智能体、但希望通过同一 CLI 入口调用它们的用户。

成功标准是：模型不能绕过代码策略；危险动作在副作用前暂停；恢复不重复已发生副作用；修改后必须通过对应修改代次的验证器才能完成；秘密不进入项目配置、日志、模型上下文或 Git；核心机制可用脚本化模拟 LLM 确定性验证。

## 3. 用户故事

### US-1：自主完成编码任务

作为开发者，我提交一个仓库任务后，希望 agent 能理解目标、检查上下文、形成下一步、修改文件、运行验证并根据结果继续，直到得到受验证的成功或明确停止原因。

验收：一次运行具有持久步骤、动作和观察记录；修改后 `finish` 受最新验证代次约束；失败、取消、预算耗尽和成功状态可区分。

### US-2：危险动作先审批

作为仓库所有者，我希望删除、联网、依赖安装、Git 写入或其他高风险动作在执行前进入 `ASK`，越界和破坏性动作进入 `DENY`。

验收：策略结果为 `ALLOW/ASK/DENY`；`ASK` 在 executor 前持久化；`DENY` 不产生工具副作用。

### US-3：跨进程恢复审批

作为用户，我希望 agent 在等待审批时退出，之后仍能查看待审批动作并安全恢复，而不是重放模型调用或前置副作用。

验收：审批绑定 run、action hash、workspace/config/policy snapshot、actor 与期限；一次消费；动作漂移、过期、重复消费均拒绝。

### US-4：客观反馈驱动修复

作为开发者，我希望 lint/test 失败被结构化回灌给下一步模型，使 agent 改变动作，而不是忽略失败并自行宣布成功。

验收：validator observation 进入 canonical transcript；重复失败有 fingerprint 与上限；通过结果只授权同一 mutation generation。

### US-5：安全使用模型凭据

作为用户，我希望使用隐藏输入、操作系统凭据库或受保护的用户配置提供 API Key，并确信状态、诊断、审计和序列化视图不会回显秘密。

验收：项目配置不能提供凭据；用户配置文件权限不合格时关闭并拒绝；`/login`、`/logout` 及凭据生命周期命令不回显值；模型提供方调用只在最终边界取得秘密。

### US-6：恢复和使用项目记忆

作为长期维护者，我希望 agent 只召回与项目相关、已接受的记忆，并能在 CLI 中查看、批准或遗忘，而不是把全部历史无界塞入 prompt。

验收：记忆按项目隔离；候选与 accepted 状态分离；召回有界；写入和展示经过秘密扫描。

### US-7：检查机制而不提供真实凭据

作为评审者，我希望运行 `guardrail`、`feedback-loop`、`approval-resume` 三个固定场景，观察真实状态机而不连接模型提供方。

验收：三个场景由 scripted/mock LLM 驱动、离线、可重复；WebUI 只暴露固定 scenario ID，无任意 prompt、路径或命令入口。

### US-8：可选委派外部智能体

作为已有用户，我希望在需要时选择外部智能体完成委派任务，同时清楚知道该通道不受自研循环的全部治理保证。

验收：委派通道与自主 Coding Agent 通道在命令、类型和 UI 说明上分离；旧入口仅作兼容，不改变产品主定位。

## 4. 范围与边界

### 4.1 本期范围

- 自主 Coding Agent 主循环与 durable run。
- 与提供方无关的单次模型边界，优先实现 OpenAI Responses 兼容协议。
- 严格类型化动作解码器与确定性工具注册表。
- 工作区路径围栏、策略、审批、执行前 barrier 和审计链。
- 文件读取/搜索/写入/补丁、受治理命令、validator、finish、记忆等动作。
- 结构化反馈、失败循环探测，以及令牌、步骤、时间和输出预算。
- canonical transcript、有界 context assembly、项目记忆和恢复目录。
- 交互式 CLI，以及外部 agent 可选委派入口。
- 固定 mock 场景 WebUI、SSE 事件和审批演示。
- Windows x86_64 / Linux x86_64 原生分发脚本与 OCI demo 镜像定义。
- GitHub Actions 作为唯一目标提交 CI；课程分支已有配置，主分支尚待合并。

### 4.2 明确不承诺

- 公网环境执行任意 prompt、仓库路径或 shell 命令。
- 完整操作系统级沙箱或对同账户管理员的防护。
- macOS 原生课程发布。
- Windows 付费 Authenticode 签名。
- 当前不存在的公网部署、托管 CI 成功、发布证明或远程干净主机通过记录。
- 把外部 agent 的内部循环描述为 Orchester 自研实现。

## 5. 领域与机制设计

### 5.1 编码智能体主循环

一次 run 的逻辑顺序为：

1. 固定 workspace、配置、策略、模型与预算 snapshot。
2. 从规范对话记录、已接受记忆、仓库索引、当前任务和最新反馈组装有界上下文。
3. 调用一次 `LanguageModel`，得到文本或候选动作。
4. 严格解码并规范化动作；未知字段、超限数据和非法组合立即成为观察结果。
5. `PolicyEngine` 计算 `ALLOW/ASK/DENY`。
6. `ASK` 持久化审批并暂停；`DENY` 回灌拒绝且不进入 executor。
7. `ALLOW` 或有效审批经过执行前屏障后调用工具。
8. 记录工具结果和修改代次；需要时运行验证器。
9. 把结果回灌下一 step，或以成功/失败/取消/预算/审批/未知结果终止。

模型不能直接执行工具，也不能自行把文本“完成了”转换为成功状态。

### 5.2 六个机制维度

| 维度 | 实现方式 | 可观察证据 |
|---|---|---|
| 工具 | 类型化注册表、严格结构、影响分类 | 非法字段拒绝；工具状态事件 |
| 治理 | 路径围栏、安全规则层级、`ASK/DENY`、执行前屏障 | `guardrail` 演示、策略和审批测试 |
| 上下文 | 规范对话记录、完整轮次、有界派生视图 | 上下文组装测试 |
| 反馈 | 验证器观察、修改代次、重复失败探测 | `feedback-loop` 演示 |
| 记忆 | 项目隔离、候选/已接受、按需召回、秘密扫描 | 记忆库与 CLI 测试 |
| 停机 | 持久终态、预算、取消、未知结果 | 恢复和运行存储测试 |

主要贡献维度是治理。审批 capability 不只绑定 call ID，还绑定规范化动作及运行环境快照，避免“批准 A、执行 B”。

### 5.3 工作区与副作用

路径检查使用规范化真实路径和工作区身份，拒绝 `..`、绝对越界、符号链接/重解析点逃逸、受保护目录和解析不确定性。字符串前缀不是安全判断依据。内置写工具、命令和验证器都参与源码观察集合；执行前后内容快照变化会递增修改代次。验证器自身若修改被观察源码，本次检查强制失败。

执行前屏障必须先持久化动作、策略、审计和已启动状态，再发出执行许可。崩溃恢复时，只有注册表声明为 `ReadOnlyIdempotent` 的读取动作可创建新尝试重试；其他已启动但未结束的动作进入未知结果，要求人工协调。

### 5.4 反馈与完成

`finish` 是受治理动作，不接受模型单方面声明。只要源码代次晚于最近通过的必需验证器，就必须重新验证。同一失败指纹或无新观察的同一动作连续达到阈值后暂停，避免无限循环。步骤数、墙钟、令牌/费用和输出大小均有硬上限。

### 5.5 审批与恢复

审批状态至少区分待处理、已批准、已拒绝、已过期、已消费和已失效。恢复从持久化步骤边界继续，不重新调用已经产出该动作的模型，也不重复执行审批前逻辑。`approval-resume` 机制演示覆盖进程重启、一次消费和动作漂移失效。

## 6. 功能模块契约

| 模块 | 输入 | 行为 | 输出 | 失败处理 |
|---|---|---|---|---|
| `ContextAssembler` | 任务、对话、记忆、仓库信号、预算 | 按固定优先级裁剪并标来源 | `ModelRequest` | 当前任务、安全策略、未决审批和最新失败不可截断 |
| `LanguageModel` | 单步模型请求 | 一次模型调用；不拥有工具循环 | 文本、动作候选、用量 | 网络、协议、限流错误结构化返回，不重放工具 |
| `ActionDecoder` | 原始模型项 | 严格结构、规范化、限长 | 一个类型化动作 | 非法输入变为观察，无副作用 |
| `PolicyEngine` | 动作与快照 | 核心硬约束后普通规则 | `ALLOW/ASK/DENY` 与规则 | 解析不确定时关闭并拒绝 |
| `ApprovalStore` | 待处理动作与人工决定 | 持久绑定、过期、一次消费 | 权限凭证或决定 | 漂移、越权、重复消费拒绝 |
| `ToolExecutor` | 绑定许可的动作 | 文件或命令副作用 | 结果与观察 | 超时、取消、未知结果分开记录 |
| `ValidatorRunner` | 工作区代次、检查项 | 执行客观检查 | 结构化报告 | 修改源码、超限或不可执行均为失败 |
| `RunStore` | 事件和状态迁移 | SQLite 原子持久化、恢复目录 | 可恢复状态或终态 | 权限不安全或迁移失败时拒绝运行 |
| `MemoryStore` | 候选、批准、查询 | 项目隔离、按需召回 | 有界记忆项 | 秘密命中拒绝；损坏内容不注入提示词 |
| `AuditSink` | 已脱敏事件 | 追加、哈希链、检查点 | 验证报告 | 审计不可用时阻止新副作用 |
| Web 适配器 | 固定场景、会话、审批 | 仅模拟的 HTTP/SSE 投影 | 界面事件 | 所有者关联、CSRF、配额、TTL、严格路由 |
| 委派适配器 | 智能体名称、任务 | 启动既有外部智能体 | 委派事件和结果 | 明确外部安全边界，不冒充自研主循环 |

## 7. 架构与数据流

```text
CLI / 模拟 WebUI
       |
应用服务
       |
自主智能体运行时 ---------------- 可选委派适配器
       |
上下文 -> 模型 -> 解码 -> 策略 -> 审批 -> 执行器
   ^                                           |
   +------ 对话记录 / 反馈 / 记忆 ------------+
       |
SQLite 运行存储 + 脱敏哈希链审计
```

主要代码落点：`kisten/laufzeit` 承载自主 agent runtime、治理、工具、反馈、记忆、配置和持久化；`kisten/konsole` 承载交互与命令路由；`kisten/server` 承载 mock-only HTTP/SSE；`kisten/protokoll` 定义版本化事件契约；`kisten/xtask` 编排测试、演示和秘密扫描。现有 crate 路径只作为代码定位，不构成产品定位。

外部依赖包括 SQLite、HTTP 客户端、OpenAI Responses 兼容提供方、Rust 工具链、Node、React、Vite、Vitest、Playwright、Docker/OCI 和可选外部智能体可执行文件。确定性核心测试不需要真实提供方或 API Key。

## 8. 数据模型与不变量

主要持久化实体的字段、关系与约束如下；表名沿用实现，字段列表聚焦安全和恢复所需的契约，不代表每个辅助字段都在此展开。

| 实体 | 关键字段 | 关系 | 关键约束 |
|---|---|---|---|
| 参与者 `actors` | `actor_id`、`kind`、`subject_hash`、`expires_at` | 被运行、审批决定和 Web 会话引用 | `actor_id` 为主键；`subject_hash` 唯一；`kind` 仅允许本地用户或 Web 会话 |
| Web 会话 `web_sessions` | `session_hash`、`actor_id`、`csrf_secret_hash`、`runs_in_window`、`active_runs`、`expires_at`、`row_version` | `actor_id` 一对一关联参与者 | 会话哈希和参与者均唯一；活动运行数最多为一；配额非负；过期后关闭访问 |
| 项目 `projects` | `project_id`、`canonical_root`、`workspace_identity` | 一个项目拥有多个运行和记忆条目 | 规范根路径和工作区身份分别唯一；路径解析不确定时拒绝运行 |
| 运行 `runs` | `run_id`、`project_id`、`owner_actor_id`、`status`、`current_step_id`、`mutation_generation`、策略/配置快照哈希、预算字段、`row_version` | 属于一个项目和一个所有者，拥有步骤、消息、事件与验证器状态 | 状态限于定义的运行状态；序号从一开始；修改代次和预算用量非负；所有恢复查询必须同时校验所有者 |
| 消息与规范对话记录 `messages`、`transcript_records` | `run_id`、顺序号、角色/类型、`call_id`、脱敏载荷或 `wire_json`、内容哈希 | 按运行有序；对话范围可由事件绑定 | 运行内顺序号唯一；载荷有大小上限；记录只追加，不允许更新或删除 |
| 步骤 `steps` | `run_id`、`step_ordinal`、`step_id`、`turn_id`、`status`、`model_call_id`、`action_id` | 属于一个运行；至多绑定一个动作 | `(run_id, step_ordinal)` 唯一；`step_id`、`action_id` 唯一；状态迁移必须符合模型、动作、审批、工具与观察顺序 |
| 动作 `actions` | `action_id`、`run_id`、`step_id`、`call_id`、`kind`、`canonical_json`、`action_hash`、`effect_class`、策略结论、`state` | 一对一关联步骤，可关联一个审批和多个按序尝试 | `step_id`、`call_id` 唯一；规范 JSON 与哈希创建后不可变；只有 `ALLOW` 或匹配审批可进入执行态 |
| 审批 `approvals` | `approval_id`、`run_id`、`action_id`、`owner_actor_id`、`state`、`action_hash`、`workspace_identity`、策略/配置快照哈希、风险、规则、期限、`capability_nonce_hash`、决定者 | 属于同一运行、动作和所有者；决定者引用参与者 | 一个动作最多一个审批；能力 nonce 哈希唯一；批准必须绑定运行、动作哈希、工作区和快照；过期、漂移或重复消费均拒绝 |
| 工具尝试 `tool_attempts` | `call_id`、`action_id`、`attempt_no`、`state`、起止时间、`observation_id` | 属于一个动作；终态必须绑定一个观察 | `(action_id, attempt_no)` 唯一；每个观察最多绑定一个尝试；未知副作用不得伪装成可安全重试 |
| 观察 `observations` | `observation_id`、`run_id`、`step_id`、`call_id`、`kind`、脱敏载荷、`fingerprint`、`outcome` | 必须与同一运行、步骤和工具调用匹配 | `observation_id` 与 `call_id` 唯一绑定；终态种类必须与尝试状态一致；记录只追加 |
| 验证器状态 `validator_states` | `run_id`、`validator_id`、`required`、`last_passed_generation`、`last_feedback_id` | 属于运行，可引用最近观察 | `(run_id, validator_id)` 唯一；通过代次非负；所有必需验证器仅对当前修改代次有效 |
| 事件 `events` 与审计检查点 `audit_checkpoints` | 运行内序号、`schema_version`、`event_id`、关联步骤/调用、`kind`、脱敏载荷；审计文件、序号、链头哈希 | 事件属于运行；检查点一对一引用事件 | `(run_id, sequence)` 和 `event_id` 唯一；事件只追加；审计序号在单个审计文件内唯一；写入失败时阻止新的副作用 |
| 对话绑定 `transcript_bindings` | `run_id`、事件序号、阶段、首尾记录序号、数量 | 将一次生命周期事件绑定到连续对话记录区间 | `(run_id, event_sequence, phase)` 唯一；区间必须连续且数量一致；绑定只追加 |
| 记忆 `memory_items` 与记忆事件 | `memory_id`、`project_id`、状态、类型、内容/哈希、来源运行、提议者、置信度、决定者 | 按项目隔离；来源运行和参与者记录来源 | 状态限于提议、接受、拒绝、遗忘；置信度位于闭区间；只有已接受且未遗忘内容可检索进入上下文；遗忘后清空正文 |

关键不变量：

- 对话记录、事件和审计只追加；序号在单次运行或日志内唯一。
- action canonical JSON 与 action hash 创建后不可变。
- action 只有在 `ALLOW` 或匹配且已消费的 approval 下才能进入 executing。
- 每个 tool attempt 有独立状态；未知结果不能被伪装成失败后安全重试。
- 验证成功绑定工作区身份、验证器集合哈希和修改代次。
- Web 运行、SSE、审批与配额查询都必须关联所有者，随机运行 ID 本身不是授权。
- 记忆只按规范项目身份召回，只有已接受内容进入模型上下文。
- SQLite 状态和 JSONL 审计用途不同；任何一方写入失败都必须有明确的停止/恢复语义。

## 9. 配置与凭据安全

### 9.1 配置层级

用户配置位于 `~/.orchester/orchester.jsonc`，项目配置位于工作区 `.orchester/project.jsonc`。用户配置可以定义模型提供方、基础 URL、模型配置档、凭据来源、治理默认值、限额、验证器、记忆、终端界面和插件。项目配置只能选择既有提供方/配置档、定义项目相关非秘密设置并继续收紧治理；它永远不能新增或覆盖 API Key、凭据来源、提供方端点，也不能放宽不可变安全约束。

### 9.2 当前实现允许的凭据来源

当前实现支持三类用户级来源：

1. `${secret:Provider}` 交给操作系统凭据后端解析，推荐用于日常使用。
2. `${env:NAME}` 引用受控进程环境；必须承认进程环境可见和注入边界。
3. 受保护的用户配置文件中的字面 API Key。该路径只有在加载器严格验证用户目录和文件的私有权限后才允许：Unix 目标为用户目录 `0700`、文件 `0600`；Windows 验证所有者和 ACL，且拒绝其他主体可读。验证失败时关闭并拒绝，错误不得包含秘密。字面值只保存在内部受保护凭据容器，普通字段、`Debug`、状态、诊断、序列化、审计和模型上下文均只显示 `<redacted>` 或来源/是否存在元数据。

项目配置中的字面 Key、`${secret:...}` 或任何间接凭据来源一律拒绝。真实 Key 也不得进入源码、Git、日志、终端历史、Web 响应、工具子进程或测试证据。

受保护用户文件方案仍是磁盘静态明文，安全性低于系统凭据库和秘密引用；权限校验只能限制普通同机账户，不能抵御同一账户恶意进程、管理员、备份泄漏或磁盘取证。因此秘密引用和操作系统凭据库保持推荐方案。

课程通用要求写明 API Key 不得进入明文配置。允许“严格权限用户配置中的字面 Key”是用户明确要求并由实现采取额外保护后的偏离，不能表述为满足该条课程原文。提交仓库中仍不得包含任何真实凭据。

### 9.3 生命周期

CLI 提供隐藏录入和状态/更新/清除能力；状态只报告来源与存在/不存在。模型提供方请求在最后边界取得秘密；日志、错误、审计、HTTP 和界面在持久化或展示前脱敏。秘密扫描同时覆盖当前树和 Git 历史中的凭据形态值。

## 10. CLI、WebUI 与可选委派

交互式 CLI 是自主 Coding Agent 的主要入口，当前可观察入口包括 `/status`、`/permissions`、`/model`、`/resume`、`/login`、`/logout`、`/plugins`、`/memory` 和 `/agent`。非交互 `self`/`delegate` 命名迁移尚未完整结束，旧 `--agent` grammar 仍存在；这是一项已知缺口，不应写成已经完成的最终 CLI 统一。

WebUI 只运行 `guardrail`、`feedback-loop`、`approval-resume` 三个固定场景，展示事件时间线、策略/验证器详情和审批恢复。它不接收自由提示词、文件路径、模型提供方配置或任意命令。会话 Cookie、CSRF、所有者关联、一次性审批权限凭证、配额和 TTL 构成公网边界，但当前只有本地实现与冒烟测试证据，没有公网 URL。

外部智能体委派保留为可选能力。委派事件与自主运行必须在展示和数据类型上区分；Orchester 不声称外部智能体内部动作受本项目治理层逐步控制。

## 11. 非功能要求

### 安全

- 默认拒绝越界和解析不确定性；硬约束不能被项目或 CLI 放宽。
- 执行、审批、恢复、审计和秘密处理均 fail closed。
- Web demo 不包含 provider credential 或任意代码执行面。
- 本项目不是完整操作系统沙箱；不可信仓库应在独立操作系统或容器边界运行。

### 可用性

- 明确区分成功、等待审批、需要 reconciliation、配置不可用和验证失败。
- 安装/卸载保留用户配置和状态，purge 必须显式确认。
- Windows x86_64 与 Linux x86_64 是计划支持的课程发布目标；macOS 不承诺。

### 可观测性

- 持久事件、结构化观察、用量、审批状态和哈希链审计可检查。
- 所有显示面遵循同一脱敏规则。
- 不以脚本存在替代真实 CI、发布、部署或远程验收结果。

### 性能与资源

- context、模型响应、工具输出、步骤、时间、token/费用和 Web run 均有上限。
- SQLite 写入和审批消费要求原子性；WebUI 有 per-session 与全局并发限额。

## 12. 技术选择

- Rust：适合显式状态机、typed action、跨平台 CLI、单二进制分发和内存/并发边界审查。
- SQLite：支持事务、恢复查询和无需独立服务的本地持久化。
- 与提供方无关的模型接口 + OpenAI Responses 优先：核心不绑定供应商，高层智能体循环仍由项目拥有。
- Web 设计与实现：视觉和交互基线采用 Open Design `design-systems/linear-app/DESIGN.md`，并按仓库记录使用对应 UI skill `skills/web-artifacts-builder/SKILL.md`；界面保持紧凑深色事件工作台、清晰层级和不可只靠颜色表达的治理状态。实现采用 React/TypeScript 与 Vite，组件和交互测试使用 Vitest，端到端验收使用 Playwright；HTTP/SSE 复用同一应用服务。
- 原生归档 + OCI：原生二进制服务本地 Coding Agent，容器只服务 mock Web demo。
- GitHub Actions：作为唯一提交 CI，运行规范项目门禁、Web 检查、容器构建和秘密扫描。

## 13. 测试、演示与当前门禁

规范总门禁为：

```powershell
cargo +stable-x86_64-pc-windows-gnu run --locked --offline --target x86_64-pc-windows-gnu -p orchester-xtask -- test-all
```

在本课程 Windows 主机上，默认 ARM64 MSVC 链接器不可用，不能用默认 `cargo` 失败替代项目结论。按 GNU 规范环境评估 `task/course-closure` 工作树时，其最近一次总门禁状态仍为失败：服务端 clippy 报 `clippy::too_many_arguments`。主分支没有该 server/xtask 收尾切片，不能用另一套局部命令代替该门禁。因此本文不声明“全部测试通过”或“最终 CI 通过”。

主分支与课程分支中已经存在的测试层合计包括：类型化解码、策略、路径、属性测试，审批、运行存储、记忆、配置安全测试，真实本地服务端冒烟测试，Web 单元/构建/浏览器检查，安装卸载测试夹具，Git 历史秘密扫描，以及三个离线机制演示。它们证明各局部机制，不自动证明主分支已合并收尾能力，也不证明整树总门禁、托管 CI、容器运行、公网部署或发布已完成。

## 14. 分发、部署与 CI 决策

`task/course-closure` 工作树包含原生归档安装/卸载脚本、校验和验证、可选 GitHub 制品证明校验路径、Dockerfile、模拟服务端、Render 部署清单和远程验收辅助脚本。Windows 制品不承诺 Authenticode；用户在验证摘要与来源后自行处理 SmartScreen，脚本不得关闭或绕过系统策略。

GitHub Actions 是选定的唯一提交 CI。课程通用要求 §4.8 明确指定 GitHub Actions，但最终清单另写 `.gitlab-ci.yml`/`unit-test`，两处措辞相互冲突。用户已明确决定只提交 GitHub Actions，并要求删除 `.gitlab-ci.yml`。课程分支已有 push/PR 测试工作流，主分支尚未合并，目前只有手动触发的发布工作流。这是用户指示的课程文字偏离，不是对 `.gitlab-ci.yml` 条款的合规声明。

当前没有以下外部证据：

- 可访问的公网 WebUI URL。
- GitHub 托管 Actions 的最终通过运行 ID/链接。
- 已发布归档、摘要、证明结果或镜像仓库摘要。
- 授权远程干净主机上的安装、运行、升级、卸载验收输出。
- Docker 守护进程上真实镜像构建/运行的最终记录。

## 15. 课程要求对照与明确偏离

| 项目 | 当前状态 | 结论 |
|---|---|---|
| 自研智能体循环与模拟 LLM 机制测试 | 已有实现和局部测试/演示 | 实质覆盖，仍受总门禁失败影响 |
| 至少三轮规格迭代 | 已记录 | 完成 |
| 实现前陌生 Agent 冷启动 | 生产实现先开始 | 历史门禁失败，不能补签 |
| 事后补偿冷启动 | 补偿检查已执行但操作纪律部分不合规；试产出已丢弃 | 不能修复时间顺序，也不构成完全合规的冷启动 |
| TDD、task 评审与过程日志 | 有多项真实记录，也有记录不完整处 | 部分完成，不做全程合规断言 |
| API Key 不进明文配置 | 用户配置允许权限保护后的字面 Key | 用户指示偏离；项目配置和 Git 仍严格禁止 |
| GitHub Actions 推送触发 CI | 课程分支配置存在，主分支未合并 | 托管运行通过证据缺失 |
| `.gitlab-ci.yml` `unit-test` | 决定移除 | 用户指示偏离，不写成合规 |
| 分发与公网 WebUI | 本地实现/脚本存在 | 发布、容器、公网、远程证据缺失 |
| `REFLECTION.md` | 已生成且披露 AI 辅助，待提交者逐段复核 | 偏离“学生本人撰写、AI 仅润色”的课程措辞 |

`REFLECTION.md` 已在文首披露 AI 参与范围；当前状态是“已生成且披露 AI 辅助，待提交者逐段复核”。提交者仍须核对仓库事实、个人判断和最终责任；不得伪造个人体验、测试结果、提交/PR、托管 CI、发布或公网证据。披露不会把该做法转换为课程原文合规，只保证提交不隐瞒 AI 使用。

## 16. 最终验收与未完成项

提交前只有在取得对应证据后才能勾选：

- [ ] 修复服务端 clippy `too_many_arguments`，规范总门禁重新运行并通过。
- [ ] GitHub 托管 Actions 最终一次运行通过并记录可核对证据。
- [ ] 在真实 Docker daemon 构建并运行镜像，执行 mock Web smoke。
- [ ] 发布 Windows/Linux x86_64 归档并记录摘要/证明。
- [ ] 在授权干净远程主机完成安装、运行、升级、卸载验收。
- [ ] 提供截止前可访问的公网 WebUI URL 并执行远端 smoke。
- [ ] 提交者逐段复核已生成且已披露 AI 辅助的 `REFLECTION.md`，确认事实、个人判断与最终责任。
- [ ] 完成最终独立文档/代码审查，确保结论与当前 commit 一致。

在上述证据缺失时，README、SPEC、SPEC_PROCESS 和提交说明必须继续使用“配置存在”“本地通过”“尚无外部证据”等精确措辞，不得用将来式脚本、manifest 或工作流文件替代真实成功记录。

## 17. 风险

- 本地治理不是 OS 级隔离；工作区内构建脚本仍可能恶意。
- 受保护用户配置中的字面 Key 仍是磁盘明文，推荐系统凭据库或秘密引用。
- SQLite 状态、审计 JSONL 和外部文件系统不能形成单一事务，崩溃恢复必须保守。
- 模型提供方协议兼容程度不同；不得静默降级或把提供方记忆当作规范事实源。
- WebUI 一旦开放自由提示词或命令会显著扩大攻击面，本期必须保持仅模拟场景。
- 当前整树存在未提交并行改动；最终证据必须绑定实际提交，而不是本工作树的暂态结果。
- 课程文字与用户决策在 CI、字面 Key、反思 AI 使用上存在明确偏离，必须由提交者承担验收风险。
