# Orchester 编码智能体说明

## 1. 产品定位

Orchester 是一个在本地终端中运行的 Coding Agent。它接收仓库开发任务，组织上下文与计划，调用 LLM 生成下一步动作，在策略约束下执行文件或命令操作，根据 lint、test 等客观反馈持续修正，最终给出经过验证的结果或明确的停止原因。

Orchester 自己拥有完整的 agent loop，包括模型调用、动作解码、策略判定、审批、执行、反馈回灌、记忆、恢复、停止条件和审计。调用 Codex、Claude Code、OpenCode 等外部智能体只是可选能力，不替代 Orchester 的核心循环。

产品采用本地优先设计。主要交互入口是 CLI，运行状态和配置保存在本机。

## 2. 问题与价值

通用编码智能体容易把模型声称的“完成”误判为工程完成，也容易在命令执行、路径越界、审批漂移、失败重试、秘密泄漏和进程恢复方面留下不可审计的隐含状态。

Orchester 解决以下问题：

- 把模型建议转换为严格类型化动作，模型不能直接执行工具。
- 在副作用发生前完成路径检查、权限判断和审批持久化。
- 用 lint、test 和自定义验证器决定任务是否可以结束。
- 在进程退出后恢复待审批动作和未结束运行。
- 对配置、事件、日志和模型上下文执行统一脱敏。
- 用固定脚本模型复现治理、反馈和恢复机制，不依赖真实凭据。

## 3. 设计原则

1. **本地优先**：任务、状态、记忆和审计默认保存在本机。
2. **最小权限**：未明确允许的动作默认拒绝，高风险动作必须审批。
3. **模型与执行分离**：LLM 只提出动作，执行权始终属于 Orchester。
4. **证据驱动完成**：源码变化后，必须通过对应修改代次的验证器。
5. **持久化后执行**：动作、策略结果和审批信息落盘后才能产生副作用。
6. **恢复不重放**：恢复流程不重复已完成的模型调用或已发生的副作用。
7. **秘密最小暴露**：凭据只在模型请求边界读取，不进入普通数据结构。
8. **委派边界明确**：外部智能体的行为与自研运行循环分开记录。

## 4. 使用场景

### 场景 1：自主完成仓库任务

操作者在仓库目录提交开发目标。Orchester 检查项目上下文，形成步骤，修改文件，运行验证，并根据结果继续处理，直到验证通过或出现明确停止原因。

验收条件：

- 运行具有持久步骤、动作和观察记录。
- 成功、失败、取消、预算耗尽和等待审批状态可以区分。
- 修改源码后，`finish` 必须受最新验证结果约束。

### 场景 2：危险动作先审批

删除文件、安装依赖、访问网络、修改版本控制状态或执行其他高风险命令时，动作必须在执行前进入审批流程。越界和破坏性动作直接拒绝。

验收条件：

- 策略结论为 `ALLOW`、`ASK` 或 `DENY`。
- `ASK` 在工具执行前持久化并暂停运行。
- `DENY` 只生成拒绝观察，不产生工具副作用。

### 场景 3：跨进程恢复

运行在等待审批时可以退出。重新启动 CLI 后，可查看待处理动作并继续运行，不需要重复此前的模型调用。

验收条件：

- 审批绑定运行、动作哈希、工作区身份、配置快照和策略快照。
- 审批凭证只能消费一次。
- 过期、动作漂移和重复消费均被拒绝。

### 场景 4：根据验证失败继续修复

lint 或 test 失败后，结构化结果进入下一步上下文。LLM 必须基于新的错误信息调整动作，不能忽略失败并自行宣布完成。

验收条件：

- 验证结果进入规范对话记录。
- 重复失败具有稳定指纹和次数上限。
- 通过结果只授权同一修改代次完成任务。

### 场景 5：安全配置模型凭据

操作者通过系统凭据库、环境引用或受保护的个人级配置提供 API Key。状态、诊断、审计和序列化结果不得回显秘密。

验收条件：

- 项目级配置不能新增或覆盖凭据。
- 个人级配置权限不安全时拒绝加载字面凭据。
- `/login`、`/logout` 和 `/status` 只显示来源与存在状态。

### 场景 6：管理项目记忆

长期维护同一仓库时，Orchester 可以保存项目约定、验证命令和已确认事实。记忆候选需要明确接受后才能进入后续模型上下文。

验收条件：

- 记忆按规范项目身份隔离。
- 候选、已接受、已拒绝和已遗忘状态分离。
- 召回数量和内容大小有上限。
- 写入和展示前执行秘密扫描。

### 场景 7：离线检查核心机制

提交者可以运行固定的治理、反馈和审批恢复场景，观察状态迁移，不连接模型提供方，也不提供 API Key。

验收条件：

- 固定场景由 scripted 或 mock LLM 驱动。
- 场景离线、可重复，并使用正式策略与持久化路径。
- 输出包括关键状态迁移、策略结论和验证结果。

### 场景 8：可选委派外部智能体

操作者可以选择外部智能体处理独立子任务。Orchester 记录委派目标、输入、进程结果和输出，但不声称能够逐步治理外部智能体内部动作。

验收条件：

- 自主运行与外部委派使用不同命令和事件类型。
- 外部进程不可绕过 Orchester 的工作目录与进程启动限制。
- 委派失败不会被记录为自主运行成功。

## 5. 范围与边界

### 5.1 范围内

- 本地 CLI Coding Agent 主循环与持久运行。
- 与提供方无关的单次模型调用接口。
- 严格类型化动作解码器和确定性工具注册表。
- 工作区路径围栏、策略、审批、执行前屏障和审计链。
- 文件读取、搜索、写入、补丁和受治理命令执行。
- 验证器、结构化反馈、失败循环探测和完成门禁。
- 步骤、时间、令牌、费用和输出大小预算。
- 规范对话记录、有界上下文组装和项目记忆。
- `/status`、`/permissions`、`/resume`、`/model`、`/plugins` 等交互命令。
- 外部智能体的可选委派入口。
- Windows x86_64 与 Linux x86_64 的本地分发。
- GitHub Actions 自动运行构建、测试和秘密扫描。

### 5.2 范围外

- 完整操作系统级沙箱。
- 对同账户高权限进程或系统管理员的防护。
- macOS 原生分发保证。
- Windows 商业代码签名。
- 对外部智能体内部循环的逐动作治理。
- 自动修改不可变安全规则。

## 6. 主循环

一次运行遵循以下顺序：

1. 固定工作区、配置、策略、模型和预算快照。
2. 从任务、仓库信号、已接受记忆和最新反馈组装有界上下文。
3. 调用一次 `LanguageModel`，得到文本或候选动作。
4. 严格解码并规范化动作；未知字段和非法组合转换为观察结果。
5. `PolicyEngine` 计算 `ALLOW`、`ASK` 或 `DENY`。
6. `ASK` 持久化审批并暂停；`DENY` 回灌拒绝信息。
7. `ALLOW` 或有效审批经过执行前屏障后调用工具。
8. 记录工具结果、修改代次和审计事件。
9. 需要时运行验证器，并把结构化结果回灌下一步骤。
10. 以成功、失败、取消、预算耗尽、等待审批或未知结果结束。

LLM 不能直接访问工具，也不能把自然语言中的“完成”转换为成功状态。

## 7. 功能模块契约

| 模块 | 输入 | 行为 | 输出 | 失败处理 |
|---|---|---|---|---|
| `ContextAssembler` | 任务、对话、记忆、仓库信号、预算 | 按固定优先级裁剪并标注来源 | `ModelRequest` | 安全策略、待处理审批和最新失败不可截断 |
| `LanguageModel` | 单步模型请求 | 完成一次模型调用，不拥有工具循环 | 文本、动作候选、用量 | 网络、协议和限流错误结构化返回 |
| `ActionDecoder` | 原始模型输出 | 严格校验、限长和规范化 | 类型化动作 | 非法输入变为观察，无副作用 |
| `PolicyEngine` | 动作与运行快照 | 先执行硬约束，再执行普通规则 | 策略结论与命中规则 | 解析不确定时关闭并拒绝 |
| `ApprovalStore` | 待处理动作与决定 | 持久绑定、过期和一次消费 | 审批能力或拒绝结果 | 漂移、越权和重复消费拒绝 |
| `ToolExecutor` | 已授权动作 | 执行文件或命令操作 | 工具结果与观察 | 超时、取消和未知结果分别记录 |
| `ValidatorRunner` | 工作区代次与检查项 | 执行客观检查 | 结构化验证报告 | 修改源码、超限或无法执行均为失败 |
| `RunStore` | 事件和状态迁移 | 事务持久化和恢复查询 | 可恢复状态或终态 | 权限不安全或迁移失败时拒绝运行 |
| `MemoryStore` | 候选、决定和查询 | 项目隔离与按需召回 | 有界记忆项 | 秘密命中拒绝，损坏内容不进入上下文 |
| `AuditSink` | 已脱敏事件 | 追加写入、哈希链和检查点 | 审计验证结果 | 审计不可用时阻止新副作用 |
| `PluginRegistry` | 插件清单和配置 | 发现、校验、启用与禁用插件 | 插件能力集合 | 清单无效或权限超界时不加载 |
| `DelegationAdapter` | 智能体名称与任务 | 启动受限外部进程 | 委派事件和结果 | 明确边界，不冒充自研主循环 |

## 8. 运行状态机

运行状态至少包括：

- `created`：运行已创建，尚未调用模型。
- `planning`：正在组装上下文或等待模型结果。
- `ready`：动作已解码，等待策略判断。
- `awaiting_approval`：高风险动作等待审批。
- `executing`：已通过执行前屏障，工具正在运行。
- `validating`：正在执行必需验证器。
- `succeeded`：当前修改代次验证通过并完成。
- `failed`：出现不可恢复错误或达到失败上限。
- `cancelled`：收到取消请求并完成安全收尾。
- `budget_exhausted`：步骤、时间、令牌或费用达到上限。
- `unknown_outcome`：副作用可能发生，但无法确认结果。

状态迁移由应用服务统一管理，工具和 LLM 不能直接写终态。

## 9. 动作与工具

### 9.1 动作结构

每个动作至少包含：

- `action_id`：动作唯一标识。
- `call_id`：模型调用内唯一标识。
- `kind`：动作类型。
- `arguments`：经过严格校验的参数。
- `effect_class`：只读、可重复写、不可重复副作用或终止。
- `canonical_json`：规范化表示。
- `action_hash`：规范化动作哈希。

动作创建后，规范化内容和哈希不可修改。

### 9.2 内置工具

- `read_file`：读取工作区内文件，限制大小和编码。
- `search_files`：按模式搜索文件名或内容，限制结果数量。
- `write_file`：创建或完整写入文件，受路径和审批规则约束。
- `apply_patch`：应用结构化补丁，并记录变更摘要。
- `run_command`：执行允许的命令，限制目录、环境、时间和输出。
- `run_validator`：执行已配置的检查项。
- `memory_propose`：提出记忆候选，不直接进入召回集合。
- `finish`：请求结束运行，必须通过完成门禁。

插件可以注册附加工具，但必须声明输入模式、影响分类、权限需求、超时和输出上限。

## 10. 工作区与策略

### 10.1 路径围栏

路径判断使用规范化真实路径和工作区身份，不使用字符串前缀作为安全依据。下列情况默认拒绝：

- `..` 逃逸和绝对路径越界。
- 符号链接或重解析点指向工作区外部。
- 受保护目录和凭据文件。
- 规范化失败或路径身份不确定。

### 10.2 策略顺序

策略按以下顺序执行：

1. 不可变硬约束。
2. 工作区路径约束。
3. 项目级收紧规则。
4. 个人级默认规则。
5. 动作影响分类和命令规则。

后置层级不能放宽前置层级。项目配置只能进一步收紧权限。

### 10.3 策略结论

- `ALLOW`：可以进入执行前屏障。
- `ASK`：必须创建持久审批并暂停。
- `DENY`：不得进入执行器，只记录拒绝观察。

## 11. 审批与恢复

审批至少绑定以下信息：

- 运行和动作标识。
- 动作哈希与影响分类。
- 工作区身份。
- 配置和策略快照哈希。
- 命中规则与风险说明。
- 决定者身份、有效期和一次性能力哈希。

审批状态包括待处理、已批准、已拒绝、已过期、已消费和已失效。恢复时从持久化步骤边界继续，不重复产生该动作的模型调用，也不重复审批前逻辑。

只有注册为 `ReadOnlyIdempotent` 的读取动作可以在崩溃后创建新尝试。其他已开始但没有结束记录的动作进入 `unknown_outcome`，需要人工核对实际副作用。

## 12. 反馈、验证与停止条件

### 12.1 修改代次

文件写入、补丁和能够影响源码的命令都会递增 `mutation_generation`。验证结果记录其对应代次。只要源码代次晚于最近通过的必需验证器，`finish` 就必须拒绝并要求重新验证。

### 12.2 验证器

验证器由项目配置选择，可以包含：

- 格式检查。
- 静态分析。
- 单元测试。
- 集成测试。
- 构建检查。
- 项目自定义只读检查。

验证器运行前后检查源码观察集合。验证器自身修改源码时，本次结果强制失败。

### 12.3 循环探测

观察结果生成稳定指纹。同一失败指纹或无新信息的同一动作连续达到阈值后，运行暂停或失败，避免无限重试。

### 12.4 预算

每次运行可以限制：

- 最大步骤数。
- 最大持续时间。
- 最大输入与输出令牌。
- 最大费用。
- 单次工具输出大小。
- 单个命令执行时间。

任一硬上限触发后停止创建新动作。

## 13. 上下文与记忆

### 13.1 规范对话记录

模型输入、模型输出、动作、工具结果和验证反馈按照运行顺序只追加保存。派生摘要可以重新生成，但不能替代规范记录。

上下文优先级从高到低为：

1. 当前任务和不可变安全规则。
2. 待处理审批与最新失败。
3. 最近完整步骤。
4. 与任务相关的仓库信号。
5. 已接受项目记忆。
6. 较早历史的有界摘要。

### 13.2 项目记忆

记忆状态包括提议、接受、拒绝和遗忘。只有已接受且未遗忘的内容可以进入模型上下文。记忆按规范项目路径和工作区身份隔离，不能仅按目录名称匹配。

适合保存的内容包括项目命令、代码约定、验证方式和经过确认的架构事实。秘密、临时错误输出和未验证推测不得保存。

## 14. 配置与凭据

### 14.1 配置位置

个人级配置位于 `~/.orchester/orchester.jsonc`，项目级配置位于工作区 `.orchester/project.jsonc`。

个人级配置可以定义：

- 模型提供方和接口地址。
- 模型配置档、采样参数和预算。
- 凭据来源。
- 默认权限和审批规则。
- 验证器、记忆和插件设置。
- 终端显示偏好。

项目级配置可以选择已有提供方或配置档，设置项目相关验证器并继续收紧权限。项目级配置不能新增凭据、覆盖接口地址或放宽硬约束。

### 14.2 凭据来源

支持以下个人级来源：

1. `${secret:Provider}`：交给操作系统凭据后端解析，作为推荐方式。
2. `${env:NAME}`：引用受控进程环境变量。
3. 个人级配置中的字面 API Key：仅在目录和文件权限满足私有要求时允许。

字面凭据只保存在受保护的内部容器中。普通配置视图、调试输出、错误、事件、审计和模型上下文只能显示 `<redacted>` 或来源元数据。

Unix 环境要求配置目录只允许所有者访问，配置文件只允许所有者读写。Windows 环境检查文件所有者和访问控制项；其他主体可读时拒绝加载字面凭据。

项目级配置中的字面 Key、秘密引用和环境引用均拒绝。真实凭据不得进入源码、版本历史、日志、终端历史、工具子进程或测试夹具。

## 15. CLI 命令

### 15.1 任务与运行

- `orchester run <task>`：启动自主 Coding Agent 运行。
- `/status`：显示运行状态、步骤、预算、验证器和待处理审批摘要。
- `/resume [run-id]`：恢复指定运行或最近可恢复运行。
- `/cancel`：请求取消并等待安全停止点。
- `/history`：查看本地运行列表和终态。

### 15.2 权限与审批

- `/permissions`：查看有效权限、规则来源和高风险动作策略。
- `/approve <approval-id>`：批准待处理动作。
- `/deny <approval-id>`：拒绝待处理动作。
- `/approvals`：列出待处理、过期和已消费审批。

审批命令必须显示规范化动作摘要、目标路径或命令、风险和命中规则，不显示凭据。

### 15.3 模型与凭据

- `/model`：查看或选择模型配置档。
- `/models`：列出可用提供方和模型。
- `/login`：通过隐藏输入或系统凭据后端保存凭据。
- `/logout`：清除指定提供方凭据。

### 15.4 插件与记忆

- `/plugins`：列出插件及其启用状态、工具和权限。
- `/plugin enable <name>`：启用通过校验的插件。
- `/plugin disable <name>`：禁用插件。
- `/memory`：查看项目记忆摘要。
- `/memory approve <id>`：接受候选记忆。
- `/memory forget <id>`：遗忘记忆。

### 15.5 外部委派

- `/agent`：列出可用外部智能体。
- `orchester delegate --agent <name> --task <text>`：执行一次受限委派。

委派命令输出必须明确标识为外部进程结果，不能与自主运行的工具观察混用。

## 16. 插件系统

插件用于扩展本地命令、工具、模型提供方适配或只读上下文来源。每个插件包含清单，至少声明：

- 名称、版本和入口。
- 能力类型。
- 配置模式。
- 请求的文件、命令或网络权限。
- 工具输入模式、影响分类、超时和输出上限。

插件默认禁用。启用前必须完成清单校验和权限确认。插件不能覆盖核心策略、审批逻辑、审计实现和完成门禁。插件卸载后，既有运行记录仍保留插件名称、版本和动作规范化数据。

## 17. 数据模型与不变量

| 实体 | 关键字段 | 关系 | 关键约束 |
|---|---|---|---|
| 项目 `projects` | `project_id`、`canonical_root`、`workspace_identity` | 一个项目拥有多个运行和记忆项 | 规范路径和工作区身份唯一；解析不确定时拒绝运行 |
| 运行 `runs` | `run_id`、`project_id`、`actor_id`、`status`、`mutation_generation`、快照哈希、预算字段、`row_version` | 属于一个项目，拥有步骤、事件和验证状态 | 状态只能取定义值；修改代次和预算用量非负 |
| 步骤 `steps` | `step_id`、`run_id`、`ordinal`、`status`、`model_call_id`、`action_id` | 属于一个运行，最多绑定一个动作 | `(run_id, ordinal)` 唯一；状态迁移遵循主循环顺序 |
| 动作 `actions` | `action_id`、`step_id`、`call_id`、`kind`、`canonical_json`、`action_hash`、`effect_class`、`state` | 可关联一个审批和多个工具尝试 | 规范内容和哈希创建后不可变；只有授权动作可执行 |
| 审批 `approvals` | `approval_id`、`action_id`、`state`、`action_hash`、工作区与快照哈希、期限、`capability_nonce_hash`、决定者 | 属于一个动作 | 一个动作最多一个有效审批；能力哈希唯一；过期、漂移和重复消费拒绝 |
| 工具尝试 `tool_attempts` | `action_id`、`attempt_no`、`state`、起止时间、`observation_id` | 属于一个动作，终态绑定一个观察 | `(action_id, attempt_no)` 唯一；未知结果不得自动重试 |
| 观察 `observations` | `observation_id`、`run_id`、`step_id`、`call_id`、`kind`、脱敏载荷、`fingerprint`、`outcome` | 与同一运行、步骤和调用匹配 | 记录只追加；终态种类必须与工具尝试一致 |
| 验证状态 `validator_states` | `run_id`、`validator_id`、`required`、`last_passed_generation`、`last_observation_id` | 属于运行，可引用最近观察 | `(run_id, validator_id)` 唯一；通过代次只能授权同一修改代次 |
| 事件 `events` | `event_id`、`run_id`、`sequence`、`schema_version`、`kind`、脱敏载荷、关联标识 | 属于一个运行 | `(run_id, sequence)` 和 `event_id` 唯一；只追加 |
| 审计检查点 `audit_checkpoints` | 审计文件标识、序号、事件标识、链头哈希 | 引用事件 | 序号唯一；链头连续；写入失败阻止新副作用 |
| 记忆 `memory_items` | `memory_id`、`project_id`、状态、类型、内容哈希、来源运行、置信度、决定者 | 按项目隔离 | 只有已接受且未遗忘内容可召回；遗忘后清空正文 |
| 插件记录 `plugin_records` | 名称、版本、清单哈希、状态、权限快照 | 被插件动作和事件引用 | 同一运行使用固定版本和权限快照 |

关键不变量：

- 对话记录、事件、观察和审计只追加。
- 动作规范化内容和动作哈希创建后不可变。
- 动作只有在 `ALLOW` 或匹配且已消费的审批下才能进入执行态。
- 每个工具尝试独立记录状态，未知结果不能伪装成安全失败。
- 验证成功绑定工作区身份、验证器集合哈希和修改代次。
- 记忆只按规范项目身份召回，只有已接受内容进入模型上下文。
- 状态数据库和追加审计承担不同职责，任一方失败都必须产生明确停止语义。

## 18. 非功能要求

### 18.1 安全

- 越界、配置不确定和路径解析失败时默认拒绝。
- 执行、审批、恢复、审计和凭据处理均采用 fail closed。
- 核心安全约束不能被项目配置、CLI 参数或插件放宽。
- 不可信仓库应在独立操作系统账户或容器边界中运行。

### 18.2 可用性

- 清楚区分成功、等待审批、未知结果、配置不可用和验证失败。
- 错误信息包含下一步可执行操作，不包含秘密。
- 安装和卸载默认保留个人级配置与运行状态，清除操作需要显式确认。

### 18.3 可观测性

- 运行状态、结构化观察、模型用量、审批和验证结果可以通过 CLI 检查。
- 审计事件包含模式版本和稳定标识。
- 所有显示面遵循同一脱敏规则。

### 18.4 性能与资源

- 上下文、模型响应、工具输出、步骤、时间、令牌和费用具有硬上限。
- 状态迁移、审批消费和步骤序号分配要求事务原子性。
- 大文件读取和搜索采用大小、数量和时间限制。

### 18.5 可移植性

- 支持 Windows x86_64 与 Linux x86_64。
- 路径、权限和进程控制使用平台适配层。
- 核心状态机、策略和 scripted LLM 测试保持平台无关。

## 19. 技术选择

- **Rust**：用于显式状态机、类型化动作、跨平台 CLI 和并发边界控制。
- **SQLite**：用于本地事务、恢复查询和单文件持久化。
- **提供方无关模型接口**：核心循环不绑定单一供应商，每次只执行一次模型调用。
- **JSONC 配置**：允许个人级配置保留注释，同时通过严格模式完成校验。
- **追加审计文件**：与状态数据库分离，形成可验证哈希链。
- **原生归档**：提供 Windows 与 Linux 命令行程序、安装脚本和校验摘要。
- **GitHub Actions**：作为唯一提交 CI，运行格式、静态分析、测试、构建和秘密扫描。

## 20. 测试策略

### 20.1 单元测试

- 动作解码与未知字段拒绝。
- 路径规范化和工作区围栏。
- 策略层级、不可变规则和影响分类。
- 上下文裁剪、记忆召回和秘密脱敏。
- 状态迁移、预算和失败指纹。

### 20.2 属性测试

- 任意路径输入不能逃逸工作区。
- 动作规范化后重复计算得到相同哈希。
- 项目级规则不能放宽硬约束。
- 日志和序列化输出不包含已知秘密。

### 20.3 集成测试

- LLM 输出动作后依次经过解码、策略、执行和反馈。
- `ASK` 在副作用前暂停，批准后只执行一次。
- 进程重启后恢复待审批运行。
- 修改代次变化使旧验证结果失效。
- 审计写入失败时阻止工具执行。
- 插件权限超界时拒绝加载。

### 20.4 离线机制测试

- `guardrail`：越界或高风险动作触发拒绝或审批。
- `feedback-loop`：验证失败进入下一步并驱动修复。
- `approval-resume`：运行退出后恢复，审批一次消费。

### 20.5 CLI 测试

- `/status` 展示运行、预算和验证状态。
- `/permissions` 展示有效规则及来源。
- `/resume` 恢复正确运行且不重放副作用。
- `/model` 切换配置档并固定运行快照。
- `/plugins` 管理插件状态和权限。
- `/login` 与 `/logout` 不回显凭据。

### 20.6 分发测试

- Windows 与 Linux 归档包含可执行文件、许可证和校验摘要。
- 安装、升级和卸载脚本在临时目录中完成往返测试。
- 卸载默认保留个人级配置与运行状态。

## 21. 验收标准

满足以下条件时，规格定义的本地 CLI Coding Agent 可以验收：

- [ ] 自主主循环能够完成“读取、修改、验证、结束”的完整任务。
- [ ] 模型无法绕过动作解码器、策略和执行前屏障。
- [ ] `ALLOW`、`ASK`、`DENY` 三种结论具有自动化测试。
- [ ] 待审批运行可以跨进程恢复，审批只能消费一次。
- [ ] 源码变化后，旧验证结果不能授权 `finish`。
- [ ] `/status`、`/permissions`、`/resume`、`/model` 和 `/plugins` 可用。
- [ ] 个人级配置支持接口地址、模型配置档和安全凭据来源。
- [ ] 项目级配置不能保存凭据或放宽安全约束。
- [ ] 项目记忆具有提议、接受、拒绝和遗忘流程。
- [ ] 插件声明权限，默认禁用，不能覆盖核心治理。
- [ ] 外部委派与自主运行在命令、事件和结果中明确分离。
- [ ] 固定离线场景能够复现治理、反馈和审批恢复机制。
- [ ] Windows 与 Linux 构建、测试和安装夹具通过。
- [ ] GitHub Actions 对推送和合并请求运行规定门禁。
- [ ] 秘密扫描覆盖仓库文件和版本历史，输出不包含真实凭据。

## 22. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 本地治理不是操作系统沙箱 | 恶意构建脚本可能访问工作区外资源 | 对不可信仓库使用独立账户或容器；默认限制命令和环境 |
| 字面 API Key 保存在磁盘 | 同账户恶意进程或高权限主体可能读取 | 推荐系统凭据库；严格检查个人级配置权限；统一脱敏 |
| 状态数据库与审计文件无法组成单一事务 | 崩溃后两侧记录可能暂时不一致 | 执行前屏障、检查点和保守恢复；不确定副作用进入未知结果 |
| 模型协议实现差异 | 动作格式或用量统计可能不一致 | 使用提供方适配层、严格解码和协议契约测试 |
| 失败反馈造成重复循环 | 消耗预算且无法推进任务 | 失败指纹、无新信息检测和硬预算上限 |
| 插件扩大权限面 | 附加工具可能绕过预期边界 | 清单校验、默认禁用、权限快照和核心策略不可覆盖 |
| 外部委派缺少逐动作治理 | 委派进程可能执行不可见操作 | 明确标识边界、限制工作目录和进程环境，优先使用自主主循环 |
