# SPEC 形成过程与事实校正

> 文档阶段：当前收尾阶段
>
> 当前结论：生产实现早于强制冷启动；事后补偿检查不能修复历史门禁；总门禁仍失败；外部交付证据仍缺失。
>
> 记录原则：逐字内容才称“对话节选”；无法确认逐字文本的内容统一标为“重建决策记录”，只概括当时选择，不伪造引语。

## 1. 输入与证据边界

本文件综合以下可复核输入：

- `AI4SE_Final_Project_通用要求.md` 与 `AI4SE_Final_Project_A_Coding_Agent_Harness.md`。
- 当前 `SPEC.md`、`PLAN.md`、`README.md`、`AGENT_LOG.md` 和 Git 提交历史。
- 主分支与 `task/course-closure` 工作树中的自主 agent、配置安全、WebUI、分发和 CI 实现。
- 规格阶段的多轮评审、补偿冷启动阶段记录，以及 Claude 会话 `c20f0da1-7448-4d88-94de-8304da367630` 的任务台账。
- 本轮对课程冲突、当前总门禁和缺失外部证据的事实校正。

本文件不把以下事项视为证据：尚未运行的工作流、仅存在的部署清单、未发布的发布脚本、未连接的远程验收脚本、没有 URL 的公网部署，以及没有运行 ID 的托管 CI。

## 2. 最初问题与关键追问

规格阶段围绕以下问题收敛：

1. 产品是外部 agent 启动器，还是必须自己实现 Coding Agent 主循环？
2. 外部智能体是核心能力、并列通道，还是可选委派能力？
3. 六个机制维度中哪一个作为主要工程贡献？
4. 模型接口只做单步调用，还是让第三方运行器拥有循环？
5. 什么动作自动允许、需要审批或硬拒绝？
6. 审批如何跨进程恢复并避免动作漂移与副作用重放？
7. 验证器失败如何进入下一轮，什么条件才允许成功结束？
8. 配置中哪些层级允许 provider、base URL 和凭据？
9. CLI 主产品与课程要求的 WebUI 如何同时满足？
10. 哪些交付结论必须有外部证据，不能由本地文件存在代替？

这些问题把项目从“统一调用现成智能体”修正为“自主编码智能体为主、外部智能体仅可选委派”，并把治理、安全、恢复和客观反馈确定为核心。

## 3. 规格迭代与对话记录

### 可核对的逐字对话节选

> 用户：“目前的首要目标是完成作业文档，其他的一切等今天交完作业之后再拓展，现在非常紧急”

该轮将工作优先级从功能拓展改为课程文档收口，并要求把未验证的交付项如实标注为未完成。

> 用户：“ci配置弄成是github action上面的测试而非.gitlab-ci.yml”

该轮确定 GitHub Actions 是唯一提交 CI，删除 GitLab CI 交付路径；同时在规格中披露这与课程最终清单中 `.gitlab-ci.yml` 条款的冲突。

> 用户：“修改为自己是一个coding agent，调用其他的agent只是其中的一个能力”

该轮最终确定产品定位：Orchester 自己是 Coding Agent，外部 agent 委派只是可选能力，不再作为产品主语。

> 用户：“那些作业文档都要中文”、“文档里面不要写日期啊”

该轮把中文化与无具体日期设为交付约束。`AGENT_LOG.md` 因此使用“阶段”顺序代替具体时间戳，并把这一点作为对课程时间戳要求的显式偏离。

### 迭代一：从外部编排改为自主编码智能体

**重建决策记录（非逐字引文）**

- Agent 追问：如果主循环仍由 Codex、Claude Code 或其他现成智能体拥有，项目如何证明符合 Project A“自行实现 harness 内核”的要求？
- 用户决定：项目按编码智能体定位，自主理解任务、规划、修改代码、执行验证、审批和恢复是主能力；外部智能体只保留为可选委派。
- 采纳结果：`LanguageModel` 只负责单次模型调用，Orchester 自己拥有上下文、解码、策略、审批、工具、反馈、记忆和停止循环。
- 否决方案：只在外部智能体外包一层审批或统一协议。原因是不能证明核心智能体循环由本项目实现。

**对 SPEC 的修订**

旧表述强调“双通道并列”；最终表述改为“自主编码智能体是产品，委派是可选能力”，并要求命令、类型、界面和安全声明明确区分两者。

### 迭代二：选择治理作为主要贡献

**重建决策记录（非逐字引文）**

- Agent 追问：若模型能输出任意 shell，如何在执行前稳定判断越界、联网、删除、依赖安装和 Git 写入？
- 用户选择：治理与安全优先，包括工作区围栏、危险动作识别、人工审批、恢复和审计。
- Agent 建议：不要用单一脏状态布尔值；把修改和验证绑定到 `mutation_generation`。
- 用户采纳：安全读取可 `ALLOW`，高风险动作 `ASK`，越界和破坏性动作 `DENY`；修改后只有同一代次的必需验证器通过才允许 `finish`。
- 否决方案：把复合 shell 字符串直接交给模型和执行器。原因是分类、规范化和审批绑定不可可靠完成。

**对 SPEC 的修订**

加入类型化动作、策略顺序、执行前屏障、持久审批权限凭证、源码观察集合、验证代次和重复失败停止条件。

### 迭代三：审批必须跨进程恢复

**重建决策记录（非逐字引文）**

- Agent 追问：用户关闭 CLI 后，审批是丢失、重新调用模型，还是从持久步骤继续？
- 用户决定：必须持久化暂停并允许以后恢复，不得重复模型调用或审批前副作用。
- Agent 建议：审批绑定运行 ID、动作哈希、工作区/配置/策略快照、参与者和期限，并且一次消费。
- 用户采纳：动作发生漂移、审批过期、跨会话越权或重复消费都拒绝。

**对 SPEC 的修订**

增加待处理、已批准、已拒绝、已过期、已消费、已失效状态，以及未知结果和人工协调语义；`approval-resume` 成为固定离线机制演示之一。

### 迭代四：配置与 API Key 边界两次修正

**可核对的用户要求摘要**

用户明确要求 `~/.orchester/orchester.jsonc` 参考本机其他编码工具的完整配置形态，并允许在该用户文件中直接写 API Key，即使这与课程“不得写入明文配置文件”的措辞冲突。

**第一阶段重建决策记录（非逐字引文）**

- Agent 最初按课程原文收紧：用户配置只写 `${secret:OpenAI}` 或 `${env:NAME}`，真实 Key 进入操作系统凭据库。
- 当时否决“仅靠 `0600`/ACL 保存字面 Key”，理由是与课程文字直接冲突。

**第二阶段事实修正**

后续实现增加受保护用户配置加载路径：只有严格验证用户目录/文件私有权限后，用户级 `orchester.jsonc` 才可含字面 API Key；权限不安全时拒绝加载，且错误、`Debug`、状态、诊断、序列化、审计和模型上下文全部脱敏。项目级 `.orchester/project.jsonc` 永远禁止字面 Key、秘密引用和其他凭据来源，也不能放宽策略。

**最终决定**

秘密引用和系统凭据库仍是推荐方案；受保护用户配置中的字面 Key 是允许但风险更高的实现边界。它是用户指示下对课程“禁止明文配置”的明确偏离，不能写成课程合规。真实 Key 仍不得进入仓库、Git 历史、日志或提交证据。

### 迭代五：CLI 主产品与 mock WebUI

**重建决策记录（非逐字引文）**

- 用户决定：本地交互式 CLI 是主要产品入口。
- Agent 指出：课程最终交付要求可访问 WebUI，但把真实仓库命令暴露到公网会扩大攻击面。
- 用户采纳：WebUI 复用同一应用服务，只开放三个固定脚本化模拟场景；不开放自由提示词、路径、模型提供方凭据或任意命令。
- 否决方案：静态落地页或公网真实 Coding Agent。前者不能证明机制，后者在课程演示范围内风险不可接受。

**对 SPEC 的修订**

加入会话所有权、CSRF、一次性审批权限凭证、配额、TTL 和所有者关联；同时明确“本地 WebUI 已有证据”不等于“公网 URL 已交付”。

### 迭代六：CI 课程文字冲突

**重建决策记录（非逐字引文）**

- 课程通用要求 §4.8 写明 GitHub Actions 必须配置；最终清单又要求 `.gitlab-ci.yml` 且包含 `unit-test`。
- 早期实现同时准备了 GitHub Actions 与 GitLab CI，以覆盖两段文字。
- 用户最终决定：GitHub Actions 是唯一提交 CI，`.gitlab-ci.yml` 删除。

**最终记录方式**

删除 GitLab 配置是用户指示的课程文字偏离，不描述为满足最终清单的 `.gitlab-ci.yml` 条款。GitHub 工作流文件存在也不等于托管 CI 已通过；必须另有真实运行证据。

### 迭代七：REFLECTION 的 AI 使用

**重建决策记录（非逐字引文）**

- 课程原文要求反思由学生本人撰写，AI 仅可辅助润色且需标注。
- 用户最终决定：允许 AI 参与提纲、初稿组织与措辞，不限于末端润色，但必须明确披露参与范围。当前文件已生成且披露 AI 辅助，待提交者逐段复核。

**最终记录方式**

这是对课程“学生本人撰写”措辞的明确偏离。反思文档必须标注哪些事实由仓库/测试提供、哪些判断由学生确认、AI 做了哪些组织或文字工作，并由学生逐项复核和承担最终责任。不得由 AI 编造个人体验、批判立场或不存在的工程证据。

## 4. 独立 SPEC 审查及修订

独立 reviewer 首轮指出五项规划阻断：

1. 普通配置覆盖与安全策略优先级冲突。
2. `ToolStarted` 后崩溃时的重试语义不清。
3. 命令和 validator 间接修改源码时，generation 没有定义。
4. 持久化只有表名，没有安全关键字段和不变量。
5. 原生 release 的签名与 Windows 首次运行处理没有决定。

采纳的修订包括：

- 把普通配置覆盖与不可放宽的安全 lattice 分开。
- 只允许 registry 内置 `ReadOnlyIdempotent` 读取创建新 attempt 重试，其余进入 unknown outcome。
- 增加 source watch set、执行前后 snapshot、validator 修改源码强制失败和结束前重扫。
- 补全 projects、runs、messages、steps、actions、approvals、attempts、observations、validators、events、audit checkpoints、actors、web sessions 和 memory 的关键关系。
- Windows/Linux release 使用 checksum 和 artifact attestation 路径，不承诺付费 Authenticode；不得关闭或绕过 SmartScreen。
- WebUI 增加 owner、CSRF、quota、TTL 和跨 session 越权测试要求。

第二轮仅剩 Web session ownership 未进入数据库关系；补入 owner/decision actor/capability nonce 和所有 owner join 后，第三轮 reviewer 返回 `Approved`。该批准只说明当时的规划规格可进入实现，不证明后续实现过程和最终交付全部合规。

## 5. Claude 会话与未完成模板任务

Claude 会话引用：`c20f0da1-7448-4d88-94de-8304da367630`。

该会话的任务台账把任务 2 标为进行中，主题为“设计完整 `.orchester` 用户目录布局和带注释的 `orchester.jsonc` 模板”。任务计划包含：

- 配置模板覆盖 `$schema`、版本、providers、model profiles、默认/评审模型、reasoning effort、治理、limits、validators、memory、TUI、features、projects trust、plugins 和 notice。
- 同时说明受保护用户文件中的字面 API Key 与 `${secret:Provider}` / `${env:NAME}` 引用。
- 将模板作为运行时配置模块常量，增加解析、往返和“无真实凭据”测试。
- 在 README 和代码注释说明 `.orchester/` 下配置、状态库、sessions、audit、memory、plugins、logs 和 skills 等布局。

该任务没有完成：任务状态未转为已完成，主工作树中现已出现 `kisten/laufzeit/tests/config_template.rs` 与 `kisten/laufzeit/src/harness/config/template.rs` 的未提交草稿，但没有可归属的完成提交或验收结果。后续工作树确实实现了受保护配置文件权限验证和凭据脱敏，但这不能反向证明“完整注释模板与目录脚手架”任务已经交付。最终 SPEC 因此只描述当前真实配置能力，不声称完整模板任务完成。

会话末尾用户再次要求“拆分成多个子任务逐步验证、提交并推送远程”，随后请求被中断/额度错误终止。该会话不能作为后续所有 task 已提交、已推送或已验收的证明。

## 6. 冷启动：历史门禁失败与事后补偿

课程要求在正式实现前，用不同类型、全新无记忆的 Agent，仅凭 `SPEC.md` 与 `PLAN.md` 尝试 1–2 个任务。实际时间顺序不是这样：智能体内核生产实现已经开始，之后才进行冷启动。因此“实现前冷启动通过”是历史上不可修复的门禁失败。

补偿冷启动阶段执行的检查如下：

- Agent：Claude Opus 5，全新 session `729ca35c-7dc1-492c-8d1d-2a0e61387467`，`plugins=[]`、`mcp=[]`。
- 输入：只提供任务 1 之前提交 `ffb6f68` 的 `SPEC.md` 与 `PLAN.md`，不提供当前实现、旧对话、记忆或口头补充。
- 隔离：一次性工作树；尝试任务 1；无提交、无合并，完成后丢弃全部试产出。
- 结果：先得到协议未解析导入的预期失败；试验实现中任务 1 新测试 `2/2`、旧协议测试 `8/8`、当时工作区测试 `79/79` 通过。这些数字只描述被丢弃的隔离工作树，不是当前树总门禁结果。
- 暴露问题：事件封装扁平/嵌套和命名冲突、任务 1 测试不足、`ApprovalRequest` 缺 `run_id`、Windows 规范工具链命令缺失、任务台账与历史提交矛盾。
- 吸收修订：后续协议、摘要、action origin、durable payload 脱敏和 PLAN 命令/ledger 得到修正。

结论是：补偿检查已执行但操作纪律部分不合规。它确实暴露了真实规格缺陷，但遇到 SPEC 与 PLAN 冲突及工具链不确定时，智能体没有暂停向用户提问，并曾自行把 PLAN 视为权威。因而它既不是完全合规的冷启动，也不能改变“生产实现先发生”的事实，不能把历史门禁由失败改成通过。

### 关键修订前后对照

第一组：协议封装与审批绑定。

- 修订前：事件封装的扁平/嵌套形状和命名不一致，`ApprovalRequest` 没有 `run_id`，无法证明审批请求与外层运行属于同一条执行链。
- 修订后：`HarnessEvent` 使用带版本、事件 ID、运行 ID、顺序号、点分 `kind` 和独立 `payload` 的固定 envelope；`ApprovalRequest.run_id` 成为必需字段，并校验其与外层 `HarnessEvent.run_id` 一致。

第二组：Windows 命令与任务台账。

- 修订前：PLAN 的字面命令没有包含本仓库所需的 Windows GNU 工具链与目标参数，任务台账也把已存在的历史提交写成未开始或未来任务。
- 修订后：规范命令明确使用 `stable-x86_64-pc-windows-gnu`、`--offline` 和 `--target x86_64-pc-windows-gnu`；任务台账区分已提交能力、未提交修订、评审缺口和外部证据阻塞，不再从计划文字反推完成状态。

## 7. 过程偏离与人工判断

| 事项 | 课程或原计划 | 实际发生 | 结论 |
|---|---|---|---|
| 生产实现前冷启动 | 必须先完成 | 实现先开始，后执行补偿检查 | 历史偏离，不能补签；补偿检查的操作纪律也部分不合规 |
| API Key 配置 | 不得进入明文配置 | 受保护用户配置允许字面 Key | 用户指示偏离；项目配置仍禁止 |
| 提交 CI | GitHub Actions 与最终清单 GitLab 文字冲突 | 仅保留 GitHub Actions | 用户指示偏离，不称 GitLab 合规 |
| REFLECTION | 学生本人撰写，AI 仅润色 | 已生成且披露 AI 辅助，待提交者逐段复核 | 用户指示偏离 |
| TDD 与逐任务双评审 | 全程要求 | 多项有证据，部分任务日志/评审不完整 | 只能声称部分执行 |
| 公网、发布、远程验收 | 最终需真实证据 | 当前无证据 | 未完成 |

人工判断的主要作用不是接受所有 AI 建议，而是处理冲突：拒绝外部运行器取代自研循环；把 WebUI 缩到仅模拟场景；把普通配置与不可放宽安全约束分层；在用户坚持字面 Key 后增加权限门禁和全视图脱敏；把仅 GitHub CI、AI 辅助反思和历史冷启动都明确记为偏离，而不是通过措辞包装成合规。

## 8. 需求探索与多智能体流程反思

`brainstorming` 最有价值的部分是持续追问“谁拥有主循环”“什么才算完成”“审批恢复是否重放副作用”“脚本存在能否算交付证据”。这些问题直接改变了产品定位、完成门禁、WebUI 威胁面和持久化不变量。

不足在于：若把每个低风险细节都拆成独立确认，过程会过长；授权 Agent 自行推进后，又容易把“计划中的任务”“工作树中的实现”和“外部已验收”混为一谈。改进办法是让每个任务结束时固定回答四个问题：改了什么、哪条测试先红后绿、谁审查、结论绑定哪个提交或外部运行。

并行子智能体适合拆分课程合规、配置安全、运行时、WebUI 和分发检查，但主智能体必须统一术语和证据等级。早期文档曾同时出现“凭据绝不进配置”与后续字面 Key 实现、“双 CI 必须存在”与用户仅 GitHub 决策、“学生本人 REFLECTION”与后续 AI 辅助决策；本轮修订的目的就是消除这些过期断言。

## 9. 当前验证状态

规范总门禁命令为：

```powershell
cargo +stable-x86_64-pc-windows-gnu run --locked --offline --target x86_64-pc-windows-gnu -p orchester-xtask -- test-all
```

默认 ARM64 MSVC 工具链在本机因链接器不可用而不能作为项目质量结论。按仓库记录的 GNU 规范环境，当前整树门禁仍失败，失败点为服务端 clippy 的 `clippy::too_many_arguments`。因此不能声明当前全部测试通过或最终 CI 为绿色。

已有局部证据包括确定性机制演示、配置安全测试、审批恢复、项目记忆、本地 server smoke、Web 测试和安装卸载 fixture。缺失项仍为：

- GitHub 托管 Actions 最终通过记录。
- 可访问公网 URL 与远端冒烟测试。
- 正式发布归档、摘要和证明。
- Docker 守护进程上的最终镜像构建/运行记录。
- 授权远程干净主机验收。
- 提交者逐段复核已生成且已披露 AI 辅助的 `REFLECTION.md`。
- 绑定最终提交的独立文档/代码审查。

## 10. 最终文档决策

本轮把 SPEC 从未来式设计改为“产品契约 + 当前实现 + 缺失证据”，并统一以下提交口径：

1. Orchester 是自主编码智能体；外部智能体只是可选委派能力。
2. 受保护用户配置可含字面 API Key，但项目配置永远禁止；推荐系统凭据库或秘密引用。
3. GitHub Actions 是唯一提交 CI；删除 GitLab 配置属于用户指示偏离。
4. `REFLECTION.md` 已生成且披露 AI 辅助，待提交者逐段复核；属于用户指示偏离。
5. 实现早于强制冷启动；补偿检查不能修复历史门禁，且其操作纪律部分不合规。
6. 当前总门禁失败在服务端 clippy `too_many_arguments`。
7. 公网 URL、托管 CI、发布、容器运行和远程验收均不得虚构。
8. Claude 会话的完整配置模板任务未完成，不得把相关计划写成已交付。

只有在实际取得新证据后，才可更新上述状态；更新时必须附命令输出摘要、commit、run ID、URL 或 artifact digest，不能只改变复选框文字。
