# SPEC 形成过程

## 1. 输入与证据边界

规格形成以以下材料为依据：

- `AI4SE_Final_Project_通用要求.md` 与 `AI4SE_Final_Project_A_Coding_Agent_Harness.md`。
- 仓库中的 `SPEC.md`、`PLAN.md`、`README.md`、`AGENT_LOG.md`、源代码、测试与提交历史。
- 已保存的测试输出、评审结论与机制演示结果。

证据分为三类：仓库事实可直接核对；过程事实只采用已留存记录；外部运行结果必须由对应平台输出证明。文件存在、计划文字和提交标题都不能单独证明功能已经验收。

## 2. 最初问题与规格收敛

规格阶段围绕以下问题展开：

1. 产品是外部 agent 启动器，还是拥有完整主循环的 Coding Agent？
2. 外部 agent 是主体能力，还是受治理的可选委派能力？
3. 六个机制维度中哪一项作为主要工程贡献？
4. 模型接口只负责单次调用，还是拥有工具循环？
5. 哪些动作自动允许、需要审批或必须拒绝？
6. 审批怎样跨进程恢复，并避免动作漂移与副作用重放？
7. 校验失败怎样进入下一模型步骤，什么条件才允许成功结束？
8. provider、模型、base URL 与凭据分别允许出现在哪一层配置中？
9. `/status`、`/permissions`、`/resume`、`/model`、`/plugins` 等命令如何形成完整 CLI 工作流？
10. 哪些结论必须有测试或平台证据，不能由实现意图代替？

这些问题将产品定位收敛为：Orchester 自身完成上下文组装、模型调用、动作解码、策略判断、审批、工具执行、验证反馈、记忆、恢复和停止；调用其他 agent 只是可选能力。治理、安全、恢复和客观反馈构成核心工程贡献。

## 3. 关键需求原话

> 需求原话：“实现完全所有的/出现的选项的功能，比如/status，/permissions，/resume，/model，/plugins等等”

该要求把 CLI 从单一启动入口扩展为可检查状态、管理权限、恢复任务、选择模型和管理插件的完整命令体系。规格因此为各命令定义稳定语义，并要求命令共享同一应用服务与持久状态。

> 需求原话：“合理规划好.orchester\orchester.jsonc”

该要求确定个人配置目录与主配置文件需要覆盖 provider、模型档案、默认模型、治理、限制、校验器、记忆、交互设置、项目信任和插件等内容，同时保持个人配置与项目配置的权限边界。

> 需求原话：“要能写ai的url和api key”

该要求推动配置边界增加受保护个人文件中的 provider 地址与字面凭据支持。字面凭据仅在文件权限满足私有性检查时加载；项目配置不得保存凭据，状态、诊断、日志、审计和模型上下文必须脱敏。

> 需求原话：“一定要拆分多任务然后逐步实现”

该要求形成按任务拆分、先失败测试、再最小实现、最后验证与评审的执行方式。任务之间通过协议、存储和应用服务接口连接，避免把全部能力集中到单次修改中。

## 4. 关键规格迭代

### 4.1 自主 Coding Agent

初始仓库已经具备外部 agent 的注册、启动和会话能力，但这些能力不能代替 Project A 要求的自研内核。规格明确 `LanguageModel` 只负责单次模型调用，Orchester 自己拥有 request → model → action → tool → feedback → stop 循环。

外部 agent 继续作为可选委派工具存在，其输出、错误、退出码和取消均受统一生命周期管理。委派失败不得被解释为主任务成功，委派进程也不能绕过工作区、策略与审计边界。

### 4.2 选择治理与安全作为主要贡献

动作被设计为类型化数据，而不是任意复合命令字符串。策略判断采用固定顺序：核心硬限制、工作区限制、项目限制、会话限制和动作分类。安全读取可以 `ALLOW`，高风险动作进入 `ASK`，越界或破坏性动作进入 `DENY`。

执行前必须同时具备持久动作、策略决定、有效许可和审计检查点。项目与会话配置只能收紧策略，不能放宽核心限制。工具执行后产生有界 observation，并在进入模型上下文前完成秘密扫描和去敏。

### 4.3 审批与恢复绑定不可变动作

审批记录绑定运行标识、动作哈希、工作区身份、配置摘要、策略摘要、参与者和有效期。审批只能消费一次；动作漂移、过期、重复消费、作用域变化或来源不一致都会使许可失效。

进程中断后从持久步骤继续，不重新调用模型生成替代动作，也不自动重放结果未知的副作用。只读且幂等的内置动作可以在新 attempt 中重试，其余未知结果进入人工协调状态。`approval-resume` 被定义为固定离线机制演示。

### 4.4 配置与凭据边界

配置分为个人层与项目层。个人 `orchester.jsonc` 可以声明 provider、模型档案、默认模型、base URL、治理偏好、资源限制和插件；项目配置只能指定项目内选项并收紧策略。

凭据优先使用 `${secret:Provider}` 或 `${env:NAME}` 引用。受保护个人配置中的字面 API Key 是受约束例外：读取前验证 Windows ACL 或 Unix 私有权限，权限不安全时直接拒绝。项目配置永远禁止字面凭据、秘密引用和其他凭据来源。任何状态输出、诊断、序列化、审计和模型输入都不得暴露秘密。

### 4.5 CLI 命令形成统一操作面

CLI 命令不分别维护状态，而是调用同一应用服务：

- `/status` 展示当前运行、模型、策略和待处理事项的去敏摘要。
- `/permissions` 展示策略来源、审批状态和有效许可，不泄露动作秘密。
- `/resume` 仅恢复满足作用域、状态和动作绑定检查的运行。
- `/model` 查看或选择已配置模型档案，不允许项目层注入凭据。
- `/plugins` 查看、启用或刷新受信插件，插件能力仍需通过统一治理管线。
- `/config`、`/login`、`/logout` 与 memory 命令补全配置、凭据和长期记忆生命周期。

这些命令共同服务于自主 Coding Agent；外部 agent 选择只是其中一个受控入口。

### 4.6 CI 路径收敛

课程通用要求明确使用 GitHub Actions，最终清单另有 GitLab 配置文字，二者存在冲突。交付路径收敛为 GitHub Actions，测试、格式、静态检查和课程文件校验均在同一平台定义。工作流文件存在不等于托管运行已经通过，最终结论仍需对应运行记录。

## 5. 独立规格审查与修订

独立审查首轮发现以下阻断问题：

1. 普通配置覆盖与不可放宽安全策略的优先级不清。
2. `ToolStarted` 后进程崩溃时的重试语义不清。
3. 命令或校验器间接修改源码时，变更代次没有统一定义。
4. 持久化设计只有表名，缺少安全关键字段、关系和不变量。
5. 原生发布的校验、证明与 Windows 首次运行处理尚未决定。

对应修订包括：

- 将普通配置覆盖与安全 restriction lattice 分开。
- 只允许 registry 内置 `ReadOnlyIdempotent` 动作创建新 attempt 重试，其余动作进入 unknown outcome。
- 增加 source watch set、执行前后快照、校验器修改源码强制失败和结束前重扫。
- 补全 projects、runs、messages、steps、actions、approvals、attempts、observations、validators、events、audit checkpoints、actors 和 memory 的关键关系。
- 发布归档使用 checksum 与 artifact attestation，不承诺付费签名，也不绕过系统安全提示。

复审确认数据关系、所有权、审批绑定和失败语义已经进入规格。评审通过表示规格可以指导实现，不代表实现、测试和外部交付自动完成。

## 6. 配置模板任务的记录边界

参考 Claude 会话中的配置模板任务包含：

- `$schema`、版本、providers、model profiles、默认与评审模型、reasoning effort。
- 治理、limits、validators、memory、TUI、features、projects trust、plugins 和 notice。
- 字面 API Key、`${secret:Provider}` 与 `${env:NAME}` 三种来源的边界说明。
- `.orchester/` 下配置、状态库、sessions、audit、memory、plugins、logs 和 skills 的目录规划。
- 模板解析、往返与“无真实凭据”测试。

记录显示该任务形成了未提交草稿，但没有完整验收证据。因此规格只描述可由当前代码和测试确认的配置能力，不把完整模板与目录脚手架记为已完成。

## 7. 冷启动检查与修订吸收

课程要求在生产实现前，让不同类型、无记忆的 agent 只依据 `SPEC.md` 与 `PLAN.md` 尝试任务。实际顺序是生产实现先开始，之后才执行隔离补偿检查，因此事前门禁未满足。

补偿检查使用全新会话、空插件与空扩展环境，只提供任务 1 之前版本的 `SPEC.md` 和 `PLAN.md`。试作位于一次性工作树，无提交、无合并，结束后丢弃产出。检查暴露了以下问题：

- 事件 envelope 的扁平与嵌套形状不一致。
- `ApprovalRequest` 缺少 `run_id`，无法证明审批与运行属于同一执行链。
- 任务 1 测试不足，协议命名存在冲突。
- Windows 规范工具链命令缺失。
- 任务台账与已有提交状态不一致。

吸收后的修订包括固定事件 envelope、补充审批运行绑定、统一 action origin、限制耐久载荷、补充去敏规则、明确 Windows GNU 离线命令，并让任务台账区分已提交能力、未提交修订、评审缺口和外部证据缺口。

补偿检查发现并修复了真实缺陷，但不能改变执行顺序。遇到 SPEC 与 PLAN 冲突或工具链不确定时没有及时暂停，也构成操作纪律偏差。

## 8. 过程偏差与证据边界

| 事项 | 原要求 | 实际记录 | 结论 |
|---|---|---|---|
| 生产实现前冷启动 | 必须先完成 | 实现开始后才执行隔离补偿检查 | 历史偏差，不能补签 |
| API Key 配置 | 课程文字倾向禁止明文配置 | 受保护个人配置允许字面 Key | 项目配置仍禁止，且必须权限检查与全链路脱敏 |
| 提交 CI | 通用要求与最终清单平台文字冲突 | 采用 GitHub Actions | 只按实际平台提供证据 |
| TDD 与逐任务双评审 | 全程执行并留痕 | 多项有测试，部分原始记录缺失 | 只能声明有证据的部分 |
| 发布与远程验收 | 需要真实运行证据 | 本地脚本与夹具不能替代外部结果 | 未有证据时不得写成完成 |

处理冲突时采用三条原则：产品身份不能由外部运行器替代；安全硬限制不能被低层配置放宽；计划、代码、测试与外部运行必须使用不同证据等级。

## 9. 多任务执行方法

多 agent 并行适合拆分课程要求核对、配置安全、运行时、机制测试和分发检查，但每个任务必须限定文件范围、输入证据、验收命令和停止条件。共享协议与存储发生变化时，应先完成接口约定，再启动依赖任务，避免并行修改同一所有权边界。

每个任务结束时固定记录四项内容：修改范围、有效红灯、绿色命令、评审与剩余缺口。提交证明代码变化，不证明当时严格执行 TDD 或外部验收；缺少原始记录时应明确写“未留存”。

Superpowers 流程中最有价值的问题是：谁拥有主循环、什么才算完成、审批恢复会不会重放副作用、脚本存在能否算交付证据。其局限是细粒度确认可能增加切换成本，因此任务拆分应以认知边界和可复核结果为准，而不是机械追求数量。

## 10. 验证原则

规范总门禁命令为：

```powershell
cargo +stable-x86_64-pc-windows-gnu run --locked --offline --target x86_64-pc-windows-gnu -p orchester-xtask -- test-all
```

验证结论必须绑定具体命令、退出码与对应提交。默认工具链不可用、依赖缺失或夹具错误属于环境问题，不能冒充功能红灯；局部测试通过也不能替代整树门禁。固定离线机制演示用于证明 guardrail、feedback-loop 和 approval-resume，provider 真实调用、托管 CI、正式发布和远程安装则需要各自独立证据。
