{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No static grants. The embedded runtime grants window controls to its exact loopback origin at startup.",
  "windows": ["main"],
  "local": false,
  "permissions": []
}
