# 课程项目反思

## Superpowers 与工程纪律

>本项目对我最大的改变，是把“让 LLM 生成代码”重新理解成“设计一个能被证据约束的工程过程”。最有价值的 Superpowers 技能首先是 brainstorming。它促使我在实现前回答产品究竟是 Coding Agent，还是仅仅包装其他代理。由此形成的核心边界是：Orchester 自己完成上下文组装、单步模型调用、严格动作解码、策略审批、工具执行、验证反馈、记忆和持久化恢复；调用其他 agent 只是可选扩展。writing-plans 的价值在于把抽象安全承诺落实到文件、失败测试、通过条件和依赖关系。verification-before-completion 同样关键，因为模型很容易把“代码已经写了”说成“任务已经完成”。缺少持续集成记录、发布制品、远程安装与验收证据时，必须如实区分实现与交付。

>也有一些环节容易形式大于实质。技能名称、固定模板和机械拆分不会自动带来正确设计，反而可能增加上下文切换。项目中最明显的问题是生产实现早于强制冷启动。后来虽用不同类型的新 agent、只提供 SPEC 与 PLAN，在一次性 worktree 中完成补偿测试并丢弃产出，发现了反馈类型、恢复语义和间接修改检测等缺陷，但不能因此声称事前门禁曾通过。流程记录的意义是让偏离、补偿及其局限可审计。

## TDD 与验证证据

>TDD 在 agent 协作中总体是放大器。它增加了前期成本，且 Rust 与 Windows GNU 工具链的红灯有时来自环境，但先观察预期失败能阻止“写完即完成”。审批恢复需要证明 capability 绑定 run、action hash、workspace、配置与时效，只能消费一次且漂移后失效；validator 闭环要证明修改使旧成功失效、失败反馈进入下一步、只有当前 generation 的必需验证全绿才能完成。否则模型很可能只实现布尔 `approved`，或把退出码零当成任务完成。若失败来自环境、夹具或过滤器错误，也不能算有效红灯。

## 子智能体的任务边界

>subagent 能稳定自主推进多久，取决于边界而不是钟表。只读研究、独立机制测试或文档核对可运行较久；同时修改协议、存储、CLI、验证策略和凭据管理很快会偏离。最佳颗粒度是一次会话回答一个问题，拥有有限文件、一个可观察失败、一个验收命令和停止条件。例如“实现动作绑定且一次性消费的审批状态机”优于“实现整个智能体”。不必机械限定二到五分钟，关键是认知范围小、结果可复核。

## 规格与计划的作用

>SPEC 与 PLAN 的质量直接决定 subagent 的偏离程度。一个具体案例是早期产品说明仍把 Orchester 描述为代理调用包装器，若按该描述实现，就会继续优化适配器，而不是建设课程要求的自研 harness 内核。brainstorming 明确“自研 Coding Agent 为主体，委派为可选能力”后，协议、运行循环和 README 才能围绕同一身份收敛。另一个歧义来自 resume：如果只写“支持恢复”，agent 可能只实现列出可恢复运行；规格必须说明哪些终态可恢复、审批暂停怎样持久化、未知副作用为何不能自动重放、恢复时不能替换原动作。补偿冷启动暴露的间接 mutation 与 observation 类型问题也说明，架构名词如果没有数据字段、不变量和失败测试，仍然不够可执行。

## 上下文与证据策略

>最有效的 prompt/context 策略是给出“事实包、边界、证据格式、停止条件”。事实包包含目标 SPEC、相关文件、已有测试与当前失败；边界明确允许修改的目录以及不得触碰的并行工作；证据格式要求报告 Red/Green、命令、退出码、diff 和未完成项；停止条件要求不确定时暂停，不得猜测或虚构外部结果。上下文不是越多越好。把整个仓库历史和所有对话塞给 agent 会稀释当前约束，更可靠的方法是保留当前任务、最新反馈、完整工具调用对和安全规则，压缩较旧内容，并通过只读 reviewer 做规格与质量两阶段检查。

>凭据与分发要求补上了“目标机器”视角。API Key 不只不能提交到 Git，也不能进入命令历史、日志、调试输出、项目配置或模型上下文；因此需要隐藏输入的 auth set/status/update/clear、系统凭据后端、引用式配置和脱敏。分发也不等于 cargo run 成功，还涉及归档校验、安装回执、可逆卸载、系统拦截、支持平台、容器权限和干净机器验证。仓库尚无真实 release 与远程安装证据，这个缺口必须保留。

## 对 Superpowers 的批判性看法

>如果重做这个项目，我会在第一行生产代码前完成并提交 SPEC、PLAN 和真正的陌生 agent 冷启动；会更早确定 Coding Agent 的核心身份，减少后续文档漂移；会把 task ledger 与 AGENT_LOG 更新作为每个 commit 的完成条件，而不是末期集中回填；还会先建立最小的 GitHub Actions 纵向切片，持续保证总门禁可运行。对 Superpowers 的批判性看法是：它假设任务能够被明确分解，测试能够代表需求，agent 能在受控上下文中工作，工程师能够及时评审。在治理状态机、协议和安全边界上这些假设大体成立；在发布账号、签名、目标机器环境和主观产品判断上则不成立。它提供的是纪律脚手架，不是正确性的自动证明。工程师不可替代的价值仍是选择问题、定义边界、识别测试遗漏、拒绝虚假的完成叙事，并对最终结果负责。
